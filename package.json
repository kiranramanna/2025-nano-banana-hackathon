{
  "dependencies": {
    "@google/genai": "^1.17.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "scripts": {
    "test:ui": "playwright test --project=chromium",
    "test:ui:headed": "playwright test --headed --project=chromium",
    "debug:ui": "PWDEBUG=1 playwright test --project=chromium"
  }
}
